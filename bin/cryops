#!/usr/bin/env bash

# Push any commits
git push

# Deploy to server is argument chosen
if [ "$1" == "deploy" ]; then
  ssh -t devraza@devraza.duckdns.org "cd /etc/nixos && git pull && sudo nixos-rebuild switch --upgrade-all"
fi
